{
  "name": "modnote",
  "version": "0.1.0",
  "description": "ModNote — AI removal-explanation composer for Reddit moderators on Devvit. Every removal, explained — in one click.",
  "license": "BSD-3-Clause",
  "private": true,
  "type": "module",
  "main": "src/main.tsx",
  "repository": {
    "type": "git",
    "url": "https://github.com/Masoud-Masoori/modnote.git"
  },
  "scripts": {
    "dev": "devvit playtest",
    "build": "tsc --noEmit && echo 'Type check passed. Run `npm run deploy` to upload to Devvit.'",
    "deploy": "devvit upload",
    "typecheck": "tsc --noEmit",
    "audit": "npm audit --omit=dev",
    "ci-install": "npm ci"
  },
  "dependencies": {
    "@devvit/public-api": "0.12.24"
  },
  "devDependencies": {
    "@devvit/tsconfig": "0.12.24",
    "@types/node": "22.10.5",
    "typescript": "5.6.3"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "//": "Versions pinned exact (no ^ / ~ / latest) per the operator's npm safety policy in D:/Claude-Coworker/OPERATOR-PROFILE.md. Bump only after audit + 30d soak."
}
